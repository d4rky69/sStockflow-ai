[project]
name = "stockflow-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mcp>=1.27,<2", "httpx>=0.28", "pydantic>=2.11"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["stockflow_mcp"]
